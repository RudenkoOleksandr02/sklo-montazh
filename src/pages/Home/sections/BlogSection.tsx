import React, {FC} from 'react';
import BlogTiles from "../../../components/containers/BlogTiles/BlogTiles";

const Blog: FC = () => {
    return (
        <section>
            <BlogTiles/>
        </section>
    );
};

export default Blog;